{
  "extends": [
    "next",
    "eslint:recommended",
    "next/core-web-vitals",
    "plugin:storybook/recommended"
  ],
  "rules": {
    // Temporary fix for Node must be provided when reporting error if location is not provided
    // TODO look into proper fix for this
    "no-unused-vars": "off"
  }
}
